name: gojo_marketplace
description: A new Flutter project for Gojo Marketplace.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0
  firebase_auth: ^4.7.0
  cloud_firestore: ^4.8.0
  google_sign_in: ^6.1.0
  provider: ^6.0.5
  lucide_icons: ^0.220.0
  google_fonts: ^5.1.0
  http: ^1.1.0
  flutter_secure_storage: ^8.0.0
  image_picker: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
